# Pin new dependencies to exact versions (no ^ / ~ prefix) on `yarn add`.
# Keeps the supply chain reproducible and blocks silent minor/patch bumps.
save-prefix ""

# Fail installs that would change yarn.lock (use in CI). Local installs can
# still update the lock intentionally with: yarn install (no flag).
# CI/build scripts already pass --frozen-lockfile explicitly.
